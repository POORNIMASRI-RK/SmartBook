{
  "name": "paperhaven-monorepo",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "install-deps": "cd Frontend && npm install && cd ../BackEnd && npm install"
  }
}
